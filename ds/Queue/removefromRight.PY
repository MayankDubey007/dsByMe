ls = [1, 2, 3, 4, 5, 6, 7, 8, 9]
print(ls)
#first way
# ls.remove(ls[0])
# ls.remove(ls[0])
# ls.remove(ls[0])
# ls.remove(ls[0])
# ls.remove(ls[0])
# print(ls)

#Second way

print(ls.pop(0))
print(ls.pop(0))
print(ls.pop(0))
print(ls.pop(0))
print(ls.pop(0))